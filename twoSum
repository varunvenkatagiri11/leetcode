class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        map = {}
        for i, num in enumerate(nums):
            search = target - num
            if search in map:
                return [map[search] ,i]
            map[num] = i
        return []
